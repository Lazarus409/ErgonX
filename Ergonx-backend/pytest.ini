[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = tests
addopts = -ra --strict-markers -p no:cacheprovider
markers =
    postgresql: requires PostgreSQL and exercises PostgreSQL-specific constraints
