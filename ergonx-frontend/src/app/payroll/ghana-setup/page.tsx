"use client";

import { useCallback, useMemo, useState } from "react";
import { CheckCircle2, Info, ShieldCheck } from "lucide-react";
import ErrorState from "@/components/ui/ErrorState";
import PageHeader from "@/components/ui/PageHeader";
import { getApiErrorMessage, payrollApi } from "@/lib/api";
import { useApiResource } from "@/lib/useApiResource";
import { MAX_PAGE_SIZE } from "@/types/api";

export default function GhanaPayrollSetupPage() {
  const [selectedKey, setSelectedKey] = useState(""); const [saving, setSaving] = useState(false); const [message, setMessage] = useState(""); const [saveError, setSaveError] = useState("");
  const load = useCallback(async () => { const [configurations, choices] = await Promise.all([payrollApi.listPayrollConfigurations(), payrollApi.getPayrollSetupChoices()]); const selectedVersion = configurations.results[0]?.selected_payroll_preset_version; const presetId = selectedVersion ?? choices.choices.find((choice) => choice.recommended)?.preset_version_id; const [contributions, thresholds] = presetId ? await Promise.all([payrollApi.listContributionRules({ preset_version: presetId, page_size: MAX_PAGE_SIZE }), payrollApi.listStatutoryThresholds({ preset_version: presetId, page_size: MAX_PAGE_SIZE })]) : [{ results: [] }, { results: [] }]; return { configurations, choices, contributions: contributions.results, thresholds: thresholds.results }; }, []);
  const { data, loading, error, reload } = useApiResource(load);
  const configuration = data?.configurations.results[0] ?? null; const choices = data?.choices; const configuredChoice = useMemo(() => choices?.choices.find((choice) => choice.preset_version_id === configuration?.selected_payroll_preset_version) ?? choices?.choices.find((choice) => choice.recommended) ?? choices?.choices[0], [choices, configuration]); const selectedChoice = choices?.choices.find((choice) => (choice.preset_version_id ?? choice.mode) === selectedKey) ?? configuredChoice;
  const save = async () => { if (!choices || !selectedChoice) return; setSaving(true); setSaveError(""); setMessage(""); try { const payload = { country_code: choices.country_code, currency: choices.currency, payroll_frequency: configuration?.payroll_frequency ?? "MONTHLY", payroll_setup_mode: selectedChoice.mode, selected_payroll_preset_version: selectedChoice.mode === "CUSTOM" ? null : selectedChoice.preset_version_id, pay_day_rule: configuration?.pay_day_rule ?? {}, rounding_rule: configuration?.rounding_rule ?? { method: "HALF_UP", decimal_places: 2 }, is_configured: true }; if (configuration) await payrollApi.updatePayrollConfiguration(configuration.id, payload); else await payrollApi.createPayrollConfiguration(payload); setMessage("Payroll configuration saved. Statutory rules remain backend-controlled."); reload(); } catch (caught) { setSaveError(getApiErrorMessage(caught)); } finally { setSaving(false); } };
  if (loading) return <main className="p-4 text-sm text-slate-500 md:p-6">Loading Ghana payroll setup...</main>;
  if (error || !data || !choices) return <main className="p-4 md:p-6"><ErrorState title="Unable to load Ghana payroll setup" message={error ?? "Payroll setup choices are unavailable."} onRetry={reload} /></main>;
  const isGhana = choices.country_code === "GH";
  return <main className="space-y-6 p-4 md:p-6"><PageHeader title="Ghana Payroll Setup" description="Choose and review the institution's Ghana payroll configuration." />{!isGhana && <ErrorState title="Ghana setup unavailable" message={`This institution is configured for ${choices.country_code}, not Ghana. Ghana payroll is selected explicitly through the payroll configuration service.`} />}{isGhana && <><section className="rounded-xl border bg-white p-5"><div className="flex gap-3"><Info size={20} className="shrink-0" /><div><h2 className="font-semibold">Configuration Choice</h2><p className="mt-1 text-sm text-slate-500">Available options are supplied by the backend for this institution. Selecting Ghana does not automatically choose a preset.</p></div></div><div className="mt-5 grid gap-4 md:grid-cols-2">{choices.choices.map((choice) => { const key = choice.preset_version_id ?? choice.mode; const selected = (selectedChoice?.preset_version_id ?? selectedChoice?.mode) === key; return <button type="button" key={key} onClick={() => setSelectedKey(key)} className={`rounded-xl border p-5 text-left ${selected ? "border-slate-900 bg-slate-50" : ""}`}>{choice.mode === "PRESET" && <ShieldCheck size={22} />}<h3 className="mt-3 font-semibold">{choice.mode === "PRESET" ? `${choice.preset_code} ${choice.version_code ?? ""}` : choice.name}</h3><p className="mt-1 text-sm text-slate-500">{choice.name}{choice.recommended && " · Recommended"}</p>{choice.compliance_warning && <p className="mt-2 text-xs text-amber-700">{choice.compliance_warning}</p>}</button>; })}</div></section><section className="rounded-xl border bg-white"><div className="border-b p-5"><h2 className="font-semibold">Backend Configuration Summary</h2><p className="text-sm text-slate-500">Read-only statutory records effective for the selected preset.</p></div><div className="divide-y"><Row label="Country / currency" value={`${choices.country_code} / ${choices.currency}`} /><Row label="Selected setup" value={selectedChoice?.mode ?? "Not selected"} /><Row label="Preset version" value={selectedChoice?.version_code ?? "Custom configuration"} /><Row label="Contribution rules" value={String(data.contributions.length)} /><Row label="Statutory thresholds" value={String(data.thresholds.length)} /></div></section><section className="rounded-xl border bg-slate-50 p-5"><h2 className="font-semibold">Important</h2><p className="mt-1 text-sm text-slate-600">Statutory thresholds, contributions, and payroll calculations are supplied and validated by the backend. This screen never calculates PAYE or statutory obligations in the browser.</p></section>{saveError && <p className="rounded border border-red-200 bg-red-50 p-3 text-sm text-red-700">{saveError}</p>}{message && <p className="flex items-center gap-2 rounded border bg-white p-3 text-sm"><CheckCircle2 size={17} />{message}</p>}<div className="flex justify-end"><button type="button" onClick={() => void save()} disabled={saving || !selectedChoice} className="rounded-lg bg-slate-900 px-5 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{saving ? "Saving..." : "Save Ghana Setup"}</button></div></>}</main>;
}
function Row({ label, value }: { label: string; value: string }) { return <div className="flex flex-col gap-1 p-4 sm:flex-row sm:items-center sm:justify-between"><span className="text-sm font-medium">{label}</span><span className="text-sm text-slate-500">{value}</span></div>; }
