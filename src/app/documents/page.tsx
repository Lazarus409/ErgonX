"use client";
import { useCallback, useState } from "react";
import EmptyState from "@/components/ui/EmptyState";
import ErrorState from "@/components/ui/ErrorState";
import LoadingState from "@/components/ui/LoadingState";
import PageHeader from "@/components/ui/PageHeader";
import StatusBadge from "@/components/ui/StatusBadge";
import { getApiErrorMessage, operationsApi } from "@/lib/api";
import { useApiResource } from "@/lib/useApiResource";
import { buttonClasses } from "@/components/ui/Button";
function formatSize(bytes: number): string { return bytes < 1024 ? `${bytes} B` : `${(bytes / 1024 / 1024).toFixed(1)} MB`; }
export default function DocumentsPage() { const load = useCallback(() => operationsApi.listDocuments(), []); const { data, loading, error, reload } = useApiResource(load); const [open, setOpen] = useState(false); const [form, setForm] = useState({ file_reference: "", original_filename: "", content_type: "application/pdf", size_bytes: "", category: "", classification: "CONFIDENTIAL" }); const [saving, setSaving] = useState(false); const [formError, setFormError] = useState(""); const save = async () => { setSaving(true); setFormError(""); try { await operationsApi.createDocument({ ...form, size_bytes: Number(form.size_bytes) || 0 }); setOpen(false); setForm({ file_reference: "", original_filename: "", content_type: "application/pdf", size_bytes: "", category: "", classification: "CONFIDENTIAL" }); await reload(); } catch (caught) { setFormError(getApiErrorMessage(caught)); } finally { setSaving(false); } }; if (loading) return <LoadingState />; if (error || !data) return <ErrorState message={error ?? "You may not have permission to view documents."} onRetry={reload} />; return <div className="space-y-6"><PageHeader title="Documents" description="Register documents against the active institution using a secure storage reference." actions={<button type="button" onClick={() => setOpen(true)} className={buttonClasses({ variant: "primary" })}>Add document</button>} /><div className="rounded-2xl border border-line bg-surface">{data.results.length === 0 ? <EmptyState title="No document records" description="Documents registered by supported ERP workflows will appear here." /> : <div className="divide-y divide-line-soft">{data.results.map((document) => <div key={document.id} className="flex items-center gap-4 p-5"><div className="min-w-0 flex-1"><p className="truncate font-medium text-ink-strong">{document.original_filename}</p><p className="mt-1 text-sm text-ink-muted">{document.category || "Uncategorised"} · {formatSize(document.size_bytes)} · {new Date(document.created_at).toLocaleString()}</p></div><StatusBadge status={document.is_active ? "ACTIVE" : "INACTIVE"} /></div>)}</div>}</div>{open && <div className="fixed inset-0 z-50 flex items-center justify-center bg-overlay p-4"><div className="w-full max-w-lg rounded-2xl bg-surface p-6 shadow-2xl"><h2 className="text-lg font-semibold">Add document</h2><p className="mt-1 text-sm text-ink-muted">Provide the reference returned by your approved storage provider.</p>{formError && <p className="mt-3 rounded-lg bg-danger-soft p-3 text-sm text-danger-ink">{formError}</p>}<div className="mt-5 grid gap-4"><Field label="Storage reference" value={form.file_reference} onChange={(value) => setForm({ ...form, file_reference: value })} /><Field label="Filename" value={form.original_filename} onChange={(value) => setForm({ ...form, original_filename: value })} /><div className="grid gap-4 sm:grid-cols-2"><Field label="Content type" value={form.content_type} onChange={(value) => setForm({ ...form, content_type: value })} /><Field label="Size (bytes)" type="number" value={form.size_bytes} onChange={(value) => setForm({ ...form, size_bytes: value })} /></div><Field label="Category" value={form.category} onChange={(value) => setForm({ ...form, category: value })} /><label className="grid gap-1 text-sm font-medium">Classification<select value={form.classification} onChange={(event) => setForm({ ...form, classification: event.target.value })} className="h-11 rounded-xl border px-3"><option>INTERNAL</option><option>CONFIDENTIAL</option><option>RESTRICTED</option></select></label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setOpen(false)} className="rounded-xl border px-4 py-2.5 text-sm font-semibold">Cancel</button><button type="button" onClick={() => void save()} disabled={saving || !form.file_reference || !form.original_filename} className={buttonClasses({ variant: "primary" })}>{saving ? "Saving…" : "Save document"}</button></div></div></div>}</div>; }
function Field({ label, value, onChange, type = "text" }: { label: string; value: string; onChange: (value: string) => void; type?: string }) { return <label className="grid gap-1 text-sm font-medium">{label}<input type={type} value={value} onChange={(event) => onChange(event.target.value)} className="h-11 rounded-xl border px-3" /></label>; }
