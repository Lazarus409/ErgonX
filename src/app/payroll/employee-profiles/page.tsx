"use client";

import { useCallback, useState } from "react";
import { Edit3, Plus, X } from "lucide-react";

import ConfirmDialog from "@/components/ui/ConfirmDialog";
import ErrorState from "@/components/ui/ErrorState";
import LoadingState from "@/components/ui/LoadingState";
import PageHeader from "@/components/ui/PageHeader";
import StatusBadge from "@/components/ui/StatusBadge";
import { employeesApi, getApiErrorMessage, payrollApi } from "@/lib/api";
import { useApiResource } from "@/lib/useApiResource";
import type { EmployeePayrollProfile } from "@/types/payroll";

type Form = { employee: string; tax_residency: "RESIDENT" | "NON_RESIDENT"; tax_identification_number: string };
const emptyForm = (): Form => ({ employee: "", tax_residency: "RESIDENT", tax_identification_number: "" });

export default function EmployeePayrollProfilesPage() {
  const load = useCallback(async () => Promise.all([payrollApi.listEmployeePayrollProfiles({ page_size: 100 }), employeesApi.listEmployees({ page_size: 100 })]), []);
  const { data, loading, error, reload } = useApiResource(load);
  const [form, setForm] = useState<Form>(emptyForm()); const [editing, setEditing] = useState<EmployeePayrollProfile | null>(null); const [open, setOpen] = useState(false); const [saving, setSaving] = useState(false); const [formError, setFormError] = useState(""); const [pending, setPending] = useState(false);
  const profiles = data?.[0].results ?? []; const employees = data?.[1].results ?? []; const employeeNames = new Map(employees.map((employee) => [employee.id, employee.full_name || employee.employee_number]));
  const openCreate = () => { setEditing(null); setForm(emptyForm()); setFormError(""); setOpen(true); };
  const openEdit = (profile: EmployeePayrollProfile) => { setEditing(profile); setForm({ employee: profile.employee, tax_residency: profile.tax_residency === "NON_RESIDENT" ? "NON_RESIDENT" : "RESIDENT", tax_identification_number: profile.tax_identification_number }); setFormError(""); setOpen(true); };
  const save = async () => {
    setSaving(true); setFormError("");
    try { if (editing) await payrollApi.updateEmployeePayrollProfile(editing.id, form); else await payrollApi.createEmployeePayrollProfile(form); setOpen(false); reload(); } catch (caught) { setFormError(getApiErrorMessage(caught)); } finally { setSaving(false); setPending(false); }
  };
  if (loading && !data) return <LoadingState />;
  if (error && !data) return <div><ErrorState title="Unable to load employee payroll profiles" message={error} onRetry={reload} /></div>;
  return <div className="space-y-6"><PageHeader title="Employee Payroll Profiles" description="Maintain the tax residency and tax identifier used by backend payroll policy." actions={<button type="button" onClick={openCreate} className="inline-flex items-center gap-2 rounded-xl bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white"><Plus className="h-4 w-4" />Add profile</button>} />{error && <ErrorState message={error} onRetry={reload} />}<section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5"><p className="font-semibold text-slate-950">Configured profiles</p><p className="mt-1 text-sm text-slate-500">A profile is required where the payroll policy depends on employee tax residency.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Employee</th><th className="px-5 py-3">Tax residency</th><th className="px-5 py-3">Tax identifier</th><th className="px-5 py-3 text-right">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{profiles.map((profile) => <tr key={profile.id}><td className="px-5 py-4 font-medium text-slate-900">{employeeNames.get(profile.employee) ?? profile.employee}</td><td className="px-5 py-4"><StatusBadge status={profile.tax_residency} /></td><td className="px-5 py-4 text-slate-600">{profile.tax_identification_number || "Not provided"}</td><td className="px-5 py-4 text-right"><button type="button" onClick={() => openEdit(profile)} className="rounded-lg border p-2 text-slate-700 hover:bg-slate-50" aria-label="Edit employee payroll profile"><Edit3 className="h-4 w-4" /></button></td></tr>)}</tbody></table>{!profiles.length && <p className="p-8 text-center text-sm text-slate-500">No employee payroll profiles are configured.</p>}</div></section>{open && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><div className="w-full max-w-lg rounded-2xl bg-white shadow-xl"><div className="flex items-start justify-between border-b p-5"><div><h2 className="font-semibold text-slate-950">{editing ? "Edit" : "Add"} payroll profile</h2><p className="mt-1 text-sm text-slate-500">The backend validates employee tenancy and controls payroll use.</p></div><button type="button" onClick={() => setOpen(false)} disabled={saving} aria-label="Close payroll profile form"><X className="h-5 w-5" /></button></div>{formError && <p className="mx-5 mt-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{formError}</p>}<div className="grid gap-4 p-5"><label><span className="text-sm font-medium">Employee</span><select value={form.employee} onChange={(event) => setForm({ ...form, employee: event.target.value })} disabled={Boolean(editing)} className="mt-1 w-full rounded-xl border px-3 py-2.5 text-sm disabled:bg-slate-50"><option value="">Select employee</option>{employees.map((employee) => <option key={employee.id} value={employee.id}>{employee.employee_number} — {employee.full_name}</option>)}</select></label><label><span className="text-sm font-medium">Tax residency</span><select value={form.tax_residency} onChange={(event) => setForm({ ...form, tax_residency: event.target.value as Form["tax_residency"] })} className="mt-1 w-full rounded-xl border px-3 py-2.5 text-sm"><option value="RESIDENT">Resident</option><option value="NON_RESIDENT">Non-resident</option></select></label><label><span className="text-sm font-medium">Tax identification number</span><input value={form.tax_identification_number} onChange={(event) => setForm({ ...form, tax_identification_number: event.target.value })} className="mt-1 w-full rounded-xl border px-3 py-2.5 text-sm" /></label></div><div className="flex justify-end gap-3 border-t p-5"><button type="button" onClick={() => setOpen(false)} disabled={saving} className="rounded-xl border px-4 py-2.5 text-sm font-semibold">Cancel</button><button type="button" onClick={() => setPending(true)} disabled={saving || !form.employee} className="rounded-xl bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{saving ? "Saving…" : "Save profile"}</button></div></div></div>}<ConfirmDialog open={pending} title={editing ? "Update payroll profile?" : "Create payroll profile?"} description="This stores the profile only after the backend validates the employee and active institution." confirmLabel="Save profile" loading={saving} onCancel={() => setPending(false)} onConfirm={() => void save()} /></div>;
}
